.waterdropCell {
	font-size: 14px;
	overflow: hidden;
	transition-property: border-color box-shadow color;
    transition-duration: 0.3s;
    transition-timing-function: linear;
	border: 1px solid steelblue;
	background-color: #fff;
	border-radius: 0px 50% 50% 50%;
	color: #557;
}

.waterdropCell:hover {
	cursor: pointer;
	box-shadow: 5px 5px 15px #667;
	color: #335;
	border-color: #335;
}

.link {
	fill: none;
	stroke: #ccc;
	stroke-width: 1.5px;
}

:focus { outline: none; }

#button {
	margin: 30px auto 0px auto;
	width: 400px;
}

#cell_map {
	width: 100%;
	height: 600px;
	/*border: 5px solid grey;*/
	margin: 30px auto 0 auto;
}

#zoom_pane {
	position: absolute;
	background-color: #f5f5f5;
	cursor: move;
}

#waterdropCells {
	position: absolute;
	width: 3280px;
	height: 3280px;
}

.placeholder {
	margin: 18% auto 0 auto;
	padding: 0;
	width: 64%;
	height: 64%;
	text-align: center;
	font-family: sans-serif;
	font-size: 1em;
}

.placeholder_zoom {
	height: 100%;
}

.cellAdd {
	width: 22px;
	height: 22px;
	display: inline-block;
	background: url(../image/sp_cell.png) no-repeat -170px -60px;
}

.cellTop {
	height: 100%;
	overflow: hidden;
	margin-bottom: 15px;
}

.content {
	margin-top: 10px;
	height: 100%;
	overflow: hidden;
}

.cell_image {
	margin: -20% 0 0 -100px;
	width: 300%;
	background-color: white;
}

#zoom_pane svg {
	position: absolute;
}

#cell {
	border-radius: 50%;
}
